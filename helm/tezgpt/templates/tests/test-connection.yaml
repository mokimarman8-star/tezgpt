apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tezgpt.fullname" . }}-test-connection"
  labels:
    {{- include "tezgpt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tezgpt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
