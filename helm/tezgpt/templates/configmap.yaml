{{- if .Values.tezgpt.configYamlContent }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tezgpt.fullname" $ }}-config
data:
  tezgpt.yaml: |
{{ .Values.tezgpt.configYamlContent | indent 4 }}

{{- end }}